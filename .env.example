# Copiar para `.env` no servidor de aplicação (192.168.1.25) antes do primeiro deploy.
# Nunca versionar `.env` real — apenas este `.env.example`.
DB_NAME=patrimonial
DB_USER=patrimonial
DB_PASSWORD=trocar-por-uma-senha-forte
