package com.fundacao.gerenciador_patrimonial.domain.enums;

/**
 * Tipo físico/administrativo do local (Lotação).
 */
public enum TipoLocal {
    /** Sala, setor ou departamento dentro de uma UPM. */
    INTERNO,

    /** Batalhão, posto de atendimento ou unidade externa. */
    EXTERNO
}
